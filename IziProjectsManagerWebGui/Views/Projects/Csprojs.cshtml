@model IEnumerable<IziHardGames.Projects.DataBase.Models.IziModelCsproj>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Module!.Guid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PathFull)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateTimeCreate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateTimeModify)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                 @Html.DisplayFor(modelItem => item.Module.Guid) 
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PathFull)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTimeCreate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTimeModify)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
